let
 var i := 3
 var j : int := (print("hola\n"); 4)
 /*var buffer : string
 function readint () : int =
   let var i := 0
       function skipto () =
         (print ("--->"); print(buffer); print("\n");
         while buffer = " " | buffer = "\n"
           do (print("J3J0\n"); buffer := getchar ()))
   in skipto (); 
      any.any := isdigit (buffer);
      while isdigit (buffer)
        do (i := i * 10 + ord (buffer) - ord ("0");
            buffer := getchar ());
      i   
   end */
in
  0
end
