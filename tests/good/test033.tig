/* a record type and a record variable */
let
  type  rectype = {name : string, age : int, weight : int}
  var rec1 : rectype := rectype {name="Nobody", age=1000, weight=56}
in
  /*rec1.name := "Somebody";
  rec1; 0*/
  0
end
