
/* A program to solve the 8-queens problem */

let
    var N := 8
    var p := 0    
    function try (c : int) =
(   for i := 0 to c do print (".");
     if c=N
     then print("Termine\n")
     else for r := 0 to N-1
	   do try(c+1) 
)
 in 
 (/*try (0);*/ 
for i := 0 to p do print (".");
 0)
end
