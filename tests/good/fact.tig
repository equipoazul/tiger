/* define a recursive function */
let
  /* calculate n! */
  function fact (n : int) : int =
    if  n > 50
      then 42
      else 56 /*n * fact (n - 1)*/

  function sumauno (n : int) : int = n + 1
in
  fact (48)
  /*sumauno(35)*/
end
