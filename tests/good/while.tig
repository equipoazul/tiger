let
  var a := 0

  function isdigit (s : string) : int = 
          let 
             var ords := ord (s)
          in
             48 <= ords & ords <= 57
          end

var buffer := getchar()
in
  while isdigit(buffer) do 
    buffer := getchar();
  0 
end
